apiVersion: v1
kind: Service
metadata:
  name: ben-todo-backend
spec:
  selector:
    app: ben-todo-backend
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.appPort }}
